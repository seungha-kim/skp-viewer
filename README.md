```
git submodule update --init --update
extern/sketchup 에 필요한 파일 준비
윈도에서 binding 폴더에 SketchUpAPI.dll 직접 복사해야 하는 문제 있음
dev-shell 실행시 working directory가 프로젝트 루트여야 함
윈도에서 runner.py config 실행 시 Debug 빌드 되어버리는 문제 있음
```
